- [x] Design Token 文件（AppColors、Spacing、AppRadius、AppTypography）与设计文档一致
- [x] `AppColors` 中的 `NSColor` 问题已修正或注明
- [x] `SettingsGroup` 背景色与页面背景有正确区分
- [x] `ChatView` 使用 `VStack` 而非 `HSplitView`
- [x] macOS 文档中已移除 iOS-only 的 `presentationDetents` 等 API
- [x] `AgentAvatarView` 代码反映实际实现（本地 > 远端 URL > 默认图标）
- [x] 设计文档中已删除所有 MPSymbol / `mp.*` 自定义图标描述
- [x] 设计文档中已补充 `/compact` 和 `/new` 命令的 UI 规范
- [x] 设计文档中已补充 `ErrorToast` 组件描述
- [x] 设计文档中已补充 `MessageDeliveryStatus` 发送状态 UI
- [x] 设计文档中已补充 `AvatarService` 头像本地存储逻辑
- [x] 设计文档中已补充 Token 用量警告及操作面板
- [x] `README.md` 中的图标资源说明已更新为当前方案
